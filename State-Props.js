STATE- State are inbuild object in ReactJs that contain some information  of your component if state change your component rerender 
       - State are mutable  
  
PROPS- Props only comunicate information between Parent component to child components (imutable )
